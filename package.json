{
  "name": "orison-pos",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first, mobile-first Point of Sale (PWA) for a small electronics retailer. Self-hosted, zero per-seat fees.",
  "scripts": {
    "start": "node server/index.js",
    "seed": "node server/seed.js",
    "dev": "node --watch server/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/static": "^7.0.4",
    "better-sqlite3": "^11.3.0",
    "fastify": "^4.28.1"
  },
  "devDependencies": {
    "puppeteer-core": "^25.10.0"
  }
}
