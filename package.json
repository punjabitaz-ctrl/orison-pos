{
  "name": "orison-pos",
  "version": "1.15.1",
  "private": true,
  "description": "Offline-first, mobile-first Point of Sale (PWA) for a small electronics retailer. Backend: Google Apps Script + Sheets + Drive. Zero per-seat fees.",
  "scripts": {
    "test": "node tests/backend-sim.mjs",
    "test:backend": "node tests/backend-sim.mjs",
    "test:client": "node --import ./tests/helpers/setup-globals.mjs --test \"tests/client-*.mjs\"",
    "test:e2e": "node tests/e2e.mjs",
    "test:pdf": "node tests/pdf-send-smoke.mjs",
    "serve": "npx --yes local-web-server --port 8080 --directory public --spa index.html",
    "test:all": "node tests/backend-sim.mjs && node --import ./tests/helpers/setup-globals.mjs --test \"tests/client-*.mjs\" && node tests/e2e.mjs && node tests/pdf-send-smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "puppeteer-core": "^25.10.0"
  }
}
