# Publishes the Orison POS demo (the app with its backend running in the
# browser, see demo/) to GitHub Pages on every push to main.
name: Demo site

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: demo-pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Demo checks
        run: node tests/demo-build.mjs
      - name: Build the demo site
        run: node scripts/build-demo.mjs dist/demo-site
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/demo-site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
